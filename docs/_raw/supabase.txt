(estado da aplicacao - via persistState) [upsert] estado serializado (AUDIT, CHK_MODELS, DESC_LIMIT, DESC_MAX_UNIDADE, CHAMADOS, etc.) :: persistState() referenciada em auditLog, uniSetGap, uniDescSalvar, chkCopyModel, chkDelModel, chamFinalizar. Nenhuma chamada _sb.from() literal neste trecho; persistencia abstraida em persistState/registrarVendaCloud.
(estado da aplicação) [upsert] UNIDADES, ESCRITORIOS, UNI_WA, MENS_CUSTOM, PACOTES, SERVICOS, CRM_LEADS, CRM_STAGES via persistState() :: persistState() é chamada em vários CRUDs (escritórios, WhatsApp, mensagens, pacotes). Implementação de persistState não está neste trecho  candidata a tabela de estado/JSON por tenant no Supabase. Nenhuma chamada direta _sb.from() aparece neste intervalo (3801-4750).
(nenhuma chamada Supabase neste trecho) [select]  :: Linhas 951-1900 contem apenas CSS final, markup HTML (sidebar/topbar/views) e handlers PWA inline. Persistencia/Supabase nao aparece neste intervalo. Inferencias de schema para o novo backend: clientes, ordens_servico, agendamentos, eventos_rede, documentos_ficha, secoes_documento, fotos_sessao, categorias_conta_pagar, categorias_conta_receber, descontos, metas, perfis_acesso, permissoes_perfil, motivos_cancelamento, automacao_nao_comparecimento, categorias_comissao, modelos_contrato, contratos_cliente, fornecedores, grupos_produto, grupos_assinatura, unidades, carteira_fidelidade. A funcao cloudCustomers sugere endpoint/RPC de 'base na nuvem' de clientes a ser ligado ao Supabase
(vendas-dashboards.html  iframe embarcado) [auth] usa o mesmo login do sistema (SSO mencionado: 'usa o mesmo login do sistema') :: buildDashb (linha 4603-4605) embute iframe vendas-dashboards.html com 'dados ao vivo (Supabase)'. As chamadas reais ao Supabase estão no arquivo embarcado, não neste trecho.
(vendas/movimento financeiro - via registrarVendaCloud) [insert] unidade, total :: pdvFinish chama registrarVendaCloud(unidade, total) se a funcao existir; integracao cloud para registro de venda. Implementacao fora deste trecho.
app_state [select] data where id=SB_ROW (.single) :: cloudLoad  restaura estado e re-render.
app_state [upsert] id (SB_ROW='laserco_prototype'), data (snapshot JSON de ~40 estruturas), updated_at :: cloudSave  debounce 1200ms.
auth [auth] signUp(email,password,options.data.nome), signInWithPassword, signOut, getSession :: Auth Supabase; conta de teste local sem backend.
customers [insert] nome, telefone, email, documento, genero, ativo, verificado, origem, unidade :: impDoImport insere em lotes de 500 quando impCloud marcado e _sb disponível
customers [insert] nome, telefone, email, documento, genero, ativo, verificado, unidade, origem (lotes de 500) :: Importação de clientes (impDoImport).
customers [insert] nome, telefone, email, documento, genero, cidade, unidade_id, origem/etiqueta :: Importacao de clientes (importModal): checkbox impCloud 'Gravar no banco de dados (Supabase · tabela customers)' default marcado; impDedup dedup por telefone ou documento; vinculo a unidade via impUni. A chamada _sb.from('customers').insert/upsert esta no JS fora deste trecho  aqui ha apenas a UI e a mencao textual explicita a Supabase/tabela customers.
customers [select] * (count exact), order id desc, ilike nome %q%, range(from,to) :: cloudCustomers  paginação CLOUD_PAGE=50; exige _sb e _authed
customers [select] * (count exact), order id desc, ilike nome, range paginação CLOUD_PAGE=50 :: cloudCustomers  base na nuvem paginada.
profiles [select] role, nome where id=uid (.single) :: onAuthed  papel do usuário pós-login.
sales_entries [select] value where unit/year/month/day (.maybeSingle) :: registrarVendaCloud  lê total do dia.
sales_entries [upsert] unit, year, month, day, value (onConflict 'unit,year,month,day') :: alimenta dashboards de vendas em tempo real; trata erro RLS/policy.